# Story 1.3: Development Tooling and Code Quality

## Status
Draft

## Story
**As a** developer,
**I want** ESLint, Prettier, and Git hooks configured for consistent code quality,
**so that** the codebase maintains high standards and follows team conventions.

## Acceptance Criteria
1. Configure ESLint with Next.js, TypeScript, and React hooks rules
2. Set up Prettier with consistent formatting (semi-colons, single quotes, 2-space indent)
3. Install and configure `lint-staged` for pre-commit code formatting
4. Create `.gitignore` with Next.js, Node.js, and IDE-specific exclusions
5. Add package.json scripts: `dev`, `build`, `start`, `lint`, `lint:fix`, `type-check`
6. Configure VSCode settings.json for automatic formatting and linting
7. Verify all tools work correctly with sample components and utilities

## Tasks / Subtasks
- [ ] Configure ESLint (AC: 1)
  - [ ] Install ESLint dependencies
  - [ ] Configure .eslintrc with Next.js rules
  - [ ] Add TypeScript ESLint rules
  - [ ] Add React hooks linting rules
- [ ] Set up Prettier (AC: 2)
  - [ ] Install Prettier
  - [ ] Create .prettierrc config (semi-colons, single quotes, 2-space)
  - [ ] Create .prettierignore file
  - [ ] Integrate Prettier with ESLint
- [ ] Configure Git hooks (AC: 3)
  - [ ] Install lint-staged
  - [ ] Install husky for pre-commit hooks
  - [ ] Configure lint-staged to run on staged files
  - [ ] Test pre-commit hook execution
- [ ] Create .gitignore (AC: 4)
  - [ ] Add Next.js build artifacts
  - [ ] Add Node.js dependencies
  - [ ] Add IDE-specific files (.vscode, .idea, etc.)
  - [ ] Add environment files
- [ ] Add package.json scripts (AC: 5)
  - [ ] dev: Next.js development server
  - [ ] build: Production build
  - [ ] start: Production server
  - [ ] lint: Run ESLint
  - [ ] lint:fix: Run ESLint with auto-fix
  - [ ] type-check: Run tsc --noEmit
- [ ] Configure VSCode settings (AC: 6)
  - [ ] Create .vscode/settings.json
  - [ ] Enable format on save
  - [ ] Configure ESLint auto-fix on save
  - [ ] Set Prettier as default formatter
- [ ] Verify tooling works (AC: 7)
  - [ ] Create test component with intentional issues
  - [ ] Verify ESLint catches errors
  - [ ] Verify Prettier formats code
  - [ ] Verify pre-commit hook runs

## Dev Notes

### Dependencies
- **Depends on:** Story 1.1 (TypeScript configuration)
- **Affects:** All future development (enforces code quality)

### Technical Requirements
- ESLint with TypeScript support
- Prettier integration with ESLint
- Husky for Git hooks
- lint-staged for pre-commit checks

### Prettier Configuration
```json
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 80
}
```

### Package.json Scripts
```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint . --ext .ts,.tsx",
    "lint:fix": "eslint . --ext .ts,.tsx --fix",
    "type-check": "tsc --noEmit",
    "prepare": "husky install"
  }
}
```

### VSCode Settings
```json
{
  "editor.formatOnSave": true,
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  },
  "editor.defaultFormatter": "esbenp.prettier-vscode"
}
```

### Testing
#### Testing Standards
- All code must pass ESLint without errors
- Prettier must format code consistently
- Pre-commit hooks must prevent commits with linting errors
- TypeScript compilation must succeed

#### Testing Approach
1. Create intentionally poorly formatted files
2. Run lint and verify errors are caught
3. Run lint:fix and verify auto-correction
4. Attempt commit and verify hook enforcement
5. Run type-check and verify TypeScript validation

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-10-11 | 1.0 | Initial story creation | Sarah (PO Agent) |

## Dev Agent Record

### Agent Model Used
_To be populated by dev agent_

### Debug Log References
_To be populated by dev agent_

### Completion Notes List
_To be populated by dev agent_

### File List
_To be populated by dev agent_

## QA Results
_To be populated by QA agent_
