# Story 1.4: Environment Configuration and Security

## Status

Done

## Story

**As a** developer,
**I want** environment variable management and security configurations,
**so that** sensitive data is protected and deployment environments are properly configured.

## Acceptance Criteria

1. Create `.env.example` with all required environment variables documented
2. Set up `.env.local` for development with placeholder values
3. Configure environment variable validation using Zod schemas in `env.mjs`
4. Add security headers in `next.config.js` (CSP, HSTS, X-Frame-Options)
5. Set up CORS configuration for API routes with proper origin restrictions
6. Configure runtime environment validation that fails fast on missing variables
7. Document environment setup process in README.md

## Tasks / Subtasks

- [x] Create .env.example file (AC: 1)
  - [x] Document DATABASE_URL
  - [x] Document CLERK\_\* variables
  - [x] Document ANTHROPIC_API_KEY
  - [x] Document E2B_API_KEY
  - [x] Document NEXT*PUBLIC*\* variables
  - [x] Add descriptive comments for each variable
- [x] Create .env.local for development (AC: 2)
  - [x] Copy from .env.example
  - [x] Add placeholder values
  - [x] Ensure file is in .gitignore
- [x] Set up environment validation (AC: 3)
  - [x] Create env.mjs with Zod schemas
  - [x] Define client and server environment schemas
  - [x] Add runtime validation on app startup
  - [x] Configure type-safe access to environment variables
- [x] Configure security headers (AC: 4)
  - [x] Add Content Security Policy (CSP)
  - [x] Add HTTP Strict Transport Security (HSTS)
  - [x] Add X-Frame-Options (prevent clickjacking)
  - [x] Add X-Content-Type-Options
  - [x] Add Referrer-Policy
- [x] Set up CORS configuration (AC: 5)
  - [x] Configure allowed origins
  - [x] Set up CORS middleware for API routes
  - [x] Add proper methods and headers configuration
- [x] Implement fail-fast validation (AC: 6)
  - [x] Validate on application startup
  - [x] Throw clear error messages for missing variables
  - [x] Prevent app from starting with invalid config
- [x] Document setup process (AC: 7)
  - [x] Update README.md with environment setup instructions
  - [x] Document how to obtain API keys
  - [x] Add troubleshooting section

## Dev Notes

### Dependencies

- **Depends on:** Story 1.1 (Next.js configuration)
- **Required for:** Stories 1.5 (database), 1.7 (Clerk), 1.11 (external services)

### Required Environment Variables

```bash
# Database
DATABASE_URL=postgresql://...

# Clerk Authentication
CLERK_SECRET_KEY=sk_test_...
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up

# AI Services
ANTHROPIC_API_KEY=sk-ant-...

# Sandbox
E2B_API_KEY=e2b_...

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

### Environment Validation Pattern (env.mjs)

```typescript
import { z } from 'zod';

const server = z.object({
  DATABASE_URL: z.string().url(),
  CLERK_SECRET_KEY: z.string().min(1),
  ANTHROPIC_API_KEY: z.string().startsWith('sk-ant-'),
  E2B_API_KEY: z.string().startsWith('e2b_'),
});

const client = z.object({
  NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: z.string().min(1),
  NEXT_PUBLIC_APP_URL: z.string().url(),
});

export const env = {
  server: server.parse(process.env),
  client: client.parse({
    NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY:
      process.env.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY,
    NEXT_PUBLIC_APP_URL: process.env.NEXT_PUBLIC_APP_URL,
  }),
};
```

### Security Headers Configuration

```javascript
// next.config.js
const securityHeaders = [
  {
    key: 'X-Frame-Options',
    value: 'SAMEORIGIN',
  },
  {
    key: 'X-Content-Type-Options',
    value: 'nosniff',
  },
  {
    key: 'Referrer-Policy',
    value: 'strict-origin-when-cross-origin',
  },
  {
    key: 'Strict-Transport-Security',
    value: 'max-age=31536000; includeSubDomains',
  },
];
```

### Testing

#### Testing Standards

- Environment validation must fail with clear error messages
- All security headers must be present in responses
- Missing required variables must prevent app startup
- Development and production configs must be tested

#### Testing Approach

1. Remove required environment variable and verify error
2. Use browser dev tools to verify security headers
3. Test CORS with different origins
4. Verify type-safety of environment variable access

## Change Log

| Date       | Version | Description                    | Author            |
| ---------- | ------- | ------------------------------ | ----------------- |
| 2025-10-11 | 1.0     | Initial story creation         | Sarah (PO Agent)  |
| 2025-10-11 | 1.1     | Story implementation completed | James (Dev Agent) |

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-5@20250929

### Debug Log References

None

### Completion Notes List

- All acceptance criteria completed successfully
- Enhanced .env.example with comprehensive documentation and comments
- Created .env.local with placeholder values
- Updated src/env.js with comprehensive Zod validation schemas including custom refinements for API key formats
- Enhanced next.config.js security headers with full CSP implementation
- Created CORS utility (src/lib/cors.ts) with environment-aware origin restrictions
- Applied CORS to tRPC API route with preflight support
- Updated README.md with detailed environment setup, API key acquisition guides, and troubleshooting section
- Linting passes successfully
- Note: Pre-existing TypeScript error in src/trpc/server.ts (unrelated to this story)

### File List

**Modified:**

- .env.example - Enhanced with all required variables and detailed documentation
- .env.local - Created with placeholder values for development
- src/env.js - Updated with comprehensive Zod schemas and validation
- next.config.js - Enhanced security headers including CSP
- src/app/api/trpc/[trpc]/route.ts - Added CORS support
- README.md - Complete rewrite with environment setup documentation

**Created:**

- src/lib/cors.ts - CORS configuration utility and middleware

## QA Results

_To be populated by QA agent_
